import { Meta } from "@storybook/blocks";
import { Sizes } from "./components";
import { fontSize } from "../../packages/cortex-core/src";

<Meta title="tokens/Font Size" />

# Font Size

You can use font size tokens with `text` prefix.

Example: `text-h1`

Example (Icon): `text-mega`

<Sizes data={fontSize} />
