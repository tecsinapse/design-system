import { Meta } from "@storybook/blocks";
import { Shapes } from "./components";
import { borderRadius } from "../../packages/cortex-core/src";

<Meta title="tokens/Border Radius" />

# Border Radius

You can use border radius tokens with `rounded` prefix.

Example: `rounded-pill`

<Shapes data={borderRadius} property={'borderRadius'} />
