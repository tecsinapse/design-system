import { PhoneInputOption } from './Option';
import { PhoneInputOptions } from './Options';
import { PhoneInputPopover } from './Popover';
import { PhoneInputRoot } from './Root';
import { PhoneInputTrigger } from './Trigger';

export const PhoneInput = {
  Root: PhoneInputRoot,
  Popover: PhoneInputPopover,
  Trigger: PhoneInputTrigger,
  Option: PhoneInputOption,
  Options: PhoneInputOptions,
};

export * from './types';
